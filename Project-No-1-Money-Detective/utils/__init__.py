"""
Money Detective utility package.

Holds small, focused helper modules used by both the command-line
analyzer (analyze_transactions.py) and the Streamlit dashboard (app.py),
so the detection/analysis logic only lives in one place.
"""
